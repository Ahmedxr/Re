# smart_chatbot.py
# ذكاء اصطناعي محادثة نصي يفهم الكلام
# By Ahmed 😎

from difflib import SequenceMatcher
import random

# قاعدة الردود
responses = {
    "هلو": "هلو حبيبي نورت 🌟",
    "شلونك": "تمام وانت شلونك؟ 😄",
    "منو انت": "اني ذكاء اصطناعي مصنوع بلغة بايثون 🤖",
    "شنو تكدر تسوي": "اكدر اجاوبك واتعلم واطور وياك! 💡",
    "السلام عليكم": "وعليكم السلام ورحمة الله وبركاته 🌙",
    "باي": "مع السلامة 👋"
}

# دالة لحساب التشابه بين الجمل
def similar(a, b):
    return SequenceMatcher(None, a, b).ratio()

print("🤖 الذكاء الاصطناعي جاهز... اكتب خروج للإنهاء\n")

while True:
    user = input("انت: ").strip().lower()
    if user == "خروج":
        print("🤖 يومك سعيد يا بطل 🌟")
        break

    best_match = None
    best_score = 0

    # يقارن كلامك ويشوف أقرب معنى
    for key in responses:
        score = similar(user, key)
        if score > best_score:
            best_score = score
            best_match = key

    # إذا التشابه أكثر من 50% يعتبر فهم
    if best_score > 0.5:
        print("🤖:", responses[best_match])
    else:
        # رد عشوائي إذا ما فهم
        fallback = [
            "مفتهمت قصدك بالضبط 🤔",
            "عيدها بغير صياغة حبي",
            "هسه ما عندي جواب 😅",
            "كلي شتريد تعرف بالضبط؟"
        ]
        print("🤖:", random.choice(fallback))
